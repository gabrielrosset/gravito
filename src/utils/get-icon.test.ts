import { ICONS } from "@/constants";
import { getIcon } from "@/utils";

describe("getIcon", () => {
  test("successful return icon", () => {
    expect(getIcon("rss")).toEqual(ICONS.rss);
    expect(getIcon("line")).toEqual(ICONS.line);
    expect(getIcon("github")).toBe(ICONS.github);
    expect(getIcon("email")).toEqual(ICONS.email);
    expect(getIcon("weibo")).toEqual(ICONS.weibo);
    expect(getIcon("twitter")).toBe(ICONS.twitter);
    expect(getIcon("gitlab")).toEqual(ICONS.gitlab);
    expect(getIcon("medium")).toEqual(ICONS.medium);
    expect(getIcon("codepen")).toEqual(ICONS.codepen);
    expect(getIcon("youtube")).toEqual(ICONS.youtube);
    expect(getIcon("facebook")).toEqual(ICONS.facebook);
    expect(getIcon("telegram")).toEqual(ICONS.telegram);
    expect(getIcon("linkedin")).toEqual(ICONS.linkedin);
    expect(getIcon("instagram")).toEqual(ICONS.instagram);
    expect(getIcon("soundCloud")).toEqual(ICONS.soundCloud);
    expect(getIcon("html5")).toEqual(ICONS.html5);
    expect(getIcon("css3")).toEqual(ICONS.css3);
    expect(getIcon("javascript")).toEqual(ICONS.javascript);
    expect(getIcon("angular")).toEqual(ICONS.angular);
    expect(getIcon("reactJs")).toEqual(ICONS.reactJs);
    expect(getIcon("sass")).toEqual(ICONS.sass);
    expect(getIcon("less")).toEqual(ICONS.less);
    expect(getIcon("gulp")).toEqual(ICONS.gulp);
    expect(getIcon("grunt")).toEqual(ICONS.grunt);
    expect(getIcon("android")).toEqual(ICONS.android);
    expect(getIcon("ios")).toEqual(ICONS.ios);
    expect(getIcon("ionic")).toEqual(ICONS.ionic);
    expect(getIcon("reactNative")).toEqual(ICONS.reactNative);
    expect(getIcon("fastlane")).toEqual(ICONS.fastlane);
    expect(getIcon("firebase")).toEqual(ICONS.firebase);
    expect(getIcon("sentry")).toEqual(ICONS.sentry);
    expect(getIcon("npm")).toEqual(ICONS.npm);
    expect(getIcon("nodeJs")).toEqual(ICONS.nodeJs);
    expect(getIcon("csharp")).toEqual(ICONS.csharp);
    expect(getIcon("java")).toEqual(ICONS.java);
    expect(getIcon("php")).toEqual(ICONS.php);
    expect(getIcon("symfony")).toEqual(ICONS.symfony);
    expect(getIcon("wordpress")).toEqual(ICONS.wordpress);
    expect(getIcon("moodle")).toEqual(ICONS.moodle);
    expect(getIcon("prestashop")).toEqual(ICONS.prestashop);
    expect(getIcon("docker")).toEqual(ICONS.docker);
    expect(getIcon("kubernetes")).toEqual(ICONS.kubernetes);
    expect(getIcon("terraform")).toEqual(ICONS.terraform);
    expect(getIcon("ansible")).toEqual(ICONS.ansible);
    expect(getIcon("jenkins")).toEqual(ICONS.jenkins);
    expect(getIcon("gcp")).toEqual(ICONS.gcp);
    expect(getIcon("azure")).toEqual(ICONS.azure);
    expect(getIcon("aws")).toEqual(ICONS.aws);
    expect(getIcon("oauth")).toEqual(ICONS.oauth);
    expect(getIcon("graphql")).toEqual(ICONS.graphql);
    expect(getIcon("elasticsearch")).toEqual(ICONS.elasticsearch);
    expect(getIcon("redis")).toEqual(ICONS.redis);
    expect(getIcon("mongodb")).toEqual(ICONS.mongodb);
    expect(getIcon("mysql")).toEqual(ICONS.mysql);
    expect(getIcon("mariadb")).toEqual(ICONS.mariadb);
    expect(getIcon("postgres")).toEqual(ICONS.postgres);
    expect(getIcon("jwt")).toEqual(ICONS.jwt);
    expect(getIcon("appSignal")).toEqual(ICONS.appSignal);
    expect(getIcon("scrollDown")).toEqual(ICONS.scrollDown);
  });
});
